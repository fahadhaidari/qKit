// starting qKit

qKit.init({
  color: '#111111',
  width: 300,
  height: 250,
  border: {
    width: 8,
    color: '#FF5555',
    radius: 4
  }
});

// note that all props are optional and can simply started as below

// qKit.init();

//  or pass any prop as desired
